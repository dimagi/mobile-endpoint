def create_xform(instance, attachments, request_meta, dao):
    xform = _get_xform_json(instance, request_meta)
    # process attachments

    return xform


def _get_xform_json(xml_string, request_meta):
    return None
